html
  head
    title="Dell-Aqcua Hidropnia"
    link( href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css' rel='stylesheet' integrity='sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx' crossorigin='anonymous')
    style.
      .thumbnail{ height: 48px; }
  body
    .container.bg-light.p-4.my-2.mx-auto
      h1.text-success.mb-5 Lista de productos
      table.table
        thead
          tr.table-stripped
            th(scope='col') Producto
            th(scope='col') Precio
            th(scope='col') Foto
        tbody
        each val in productos
          tr.table-success.align-middle
            td= `${val.title}`
            td= `$${val.price}`
            td 
              img.thumbnail(src=`${val.thumbnail}` alt='')
      a.btn.btn-success(href='/' role='button') Volver al formulario